[Unit]
Description={{ .PorjectName }}
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/local/bin/server -workdir /usr/local/share/server -config /etc/server/config.yml
Restart=on-failure

[Install]
WantedBy=multi-user.target